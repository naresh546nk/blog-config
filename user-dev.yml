server:
  port: 8000

  application:
    name: user
  datasource:
    url: jdbc:mysql://database-mysql:3306/blogsite?createDatabaseIfNotExist=true
    username: admin
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
  config:
    import: configserver:http://configserver:8100/
axon:
  axonserver:
    servers: axonserver:8124


